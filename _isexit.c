#include <stdio.h>
#include <stdlib.h>
#include <stdbool.h>
#include "main.h"
/**
 * _isexit - checks if a user enters exit as a command
 * @str: the string entered on the command line
 * Return: true or false
 */
bool _isexit(char **str)
{
	if (_strcmp(str[0], "exit") == 0)
	{
		
	}
	return (true);
}
